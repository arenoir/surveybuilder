<%= form_for(@crisis) do |f| %>
  <% if @crisis.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@crisis.errors.count, "error") %> prohibited this crisis from being saved:</h2>

      <ul>
      <% @crisis.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :country %><br>
    <%= f.text_field :country %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.datetime_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
